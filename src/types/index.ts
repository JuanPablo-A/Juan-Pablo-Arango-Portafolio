// Definition of the types used in the project
export type Lenguage = {
    id: string,
    name: string,
    progress: number
}

// Definition of the types used in the project
export type Item= {
    id: string,
    img: string,
    title: string,
    description: string
}